extends Node

signal set_picker_entity( cat_and_id )

signal event_selected( event )
signal change_snap( subdiv )
signal change_snap_rel( subdiv )

signal chart_loaded()

signal command_log( string )
